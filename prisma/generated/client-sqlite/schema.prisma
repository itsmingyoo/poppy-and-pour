generator client {
  provider = "prisma-client-js"
  output   = "./generated/client-sqlite"
}

datasource sqlite {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Test {
  id    Int     @id @default(autoincrement())
  email String  @unique
  text  String?
}
